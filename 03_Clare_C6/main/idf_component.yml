## IDF Component Manager Manifest File
dependencies:
  # TEMP EXPERIMENT: pin to the exact release the S3 reference product uses.
  # 1.8.0 (resolved by ^1.5.0) stalls every WSS TLS handshake after the first
  # HTTP session on this target while 1.5.0 is proven in production.
  espressif/esp_websocket_client: ==1.5.0
  espressif/esp_codec_dev: ^1.4.0
  espressif/esp_lcd_sh8601: ^1.0.0
  espressif/esp_audio_effects: ^1.4.2
  espressif/esp_audio_codec: "^2.4.0"
  ## Required IDF version
  idf:
    version: '>=4.1.0'
  lvgl/lvgl: ^9.5.0
  waveshare/esp_lcd_touch_cst9217: ^1.0.4
  espressif/esp_lvgl_adapter: ^0.3.0
